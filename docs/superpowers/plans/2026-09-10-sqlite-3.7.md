# 原生SQLite 3.7.17兼容修复

用户明确要求继续使用现有SQLite 3.7.17，不安装驱动或更新SQLite。Python为3.10.13、服务器x86_64。撤回pysqlite3依赖和选择驱动逻辑；本机仅曾下载wheel，未安装，不随修复包交付。

## 合同

- 所有数据库继续使用Python标准库sqlite3及现有文件，不修改系统库，不全局替换sys.modules。
- 注册普通Python标量函数处理项目所需JSON字段、字符串包含等，不使用deterministic参数或SQLite JSON1。
- 准确性查询移除CTE/窗口函数：只读连接现有库，在临时表中筛选，用排序游标一次扫描选出各组最新预测，分批存储选中键；P95用有序临时数据及普通聚合得到nearest-rank，保持同口径/同分母。不得全量载入Python列表或退化为全表两两比较。
- 成效汇总、兑现报告和启用评估同样移除WITH/OVER；样本upsert改为事务内旧语法INSERT OR IGNORE与有条件UPDATE，继续保留首个有效值和冲突检测。
- 保留快照一致事务、源数据库只读、全部候选/选中点导出、坏值/零值规则和执行门控。缺数据为空，SQL失败显示错误并留详细日志。
- 测试通过SQL语法守卫拒绝WITH/OVER/新UPSERT/deterministic，比较现代SQLite路径既有数值、去重/容器口径、旧文件往返、原子性；全量回归及部署包校验。

- [x] 原生连接及旧SQL查询实现。
- [x] 关联报表/留档SQL兼容与回归。
- [x] 全量检查、文档和不含新SQLite依赖的部署包。

## 验证结果

本机全量731 passed、33 subtests passed，保留既有NumPy非有限输入警告；compileall、pyflakes、vulture80、git diff --check通过。旧SQL守卫覆盖CTE/窗口/JSON1/新UPSERT/部分索引/函数flag禁用，源URI只读（模拟忽略query_only）、999参数、1021点P95、跨库ID及长规格元数据。大段JSON只按曲线存一次，不复制进临时点表。

生成 `dist/resource_predict_20260910_155332.zip`，100个文件，ZIP CRC和逐文件源码一致性通过；不含wheel和pysqlite3依赖，仅使用标准库sqlite3。此前只下载未安装的wheel未使用。服务器实际3.7.17环境仍需部署验证；本机未用新驱动冒充旧版实测。保留其他工作区修改，未提交远程，缓存删除沿用此前自动审批限制。

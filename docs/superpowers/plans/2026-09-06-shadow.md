# 影子对照实施计划

1. 新增 pipeline/shadow.py：仅对完整新预测生成上界方案，冻结正式与影子 action/target_spec/policy_tier，生成容器或 VM 资源量与比例基准。
2. 新增 pipeline/shadow_evaluation.py：按批次导入影子快照与曲线预算，SQL 配对汇总、相邻建议变更率、跳过原因及保留期级联删除。
3. pipeline/run.py 在校准后、留档前生成影子对象；forecast_archive.py 留档；realized_error.py 导入及报告；partial.py 不复用旧配对；write_outputs.py 输出摘要。
4. tests/test_shadow.py 覆盖现有策略不变、完整性、配对数值、容器预算、副本变更、重复导入、相邻变更、保留期及局部重算。更新 docs 中配置、架构、API、开发验证说明。
5. 执行 .venv/Scripts/python.exe 的 compileall、pyflakes、vulture --min-confidence 80、pytest -q，检查 diff 并清理缓存；保留既有未提交工作。

## 完成记录

五项工作已完成。本轮增加 10 项测试，全量结果为 451 passed、28 subtests passed；保留基线已有的一个 NumPy 非有限输入警告。compileall、pyflakes、vulture --min-confidence 80 和 git diff --check 均通过。

已实现两套建议冻结留档、同一真实观测集合的配对评分、资源量差异、建议变更频率及跳过原因。正式建议与执行门控未改为使用影子方案。生产覆盖率、资源收益及万级吞吐尚未实测；本轮和此前未提交改动继续保留在当前分支工作区，未提交、未推送。

# 版本更新说明

## 2026-09-11：调配后首次采集即展示成效

- 调配成效默认采用版本2政策：调配后的下一次有效采集确认目标规格、使用量和容量后，立即展示前后对比并计入汇总，不等待1小时稳定期或24小时观察窗口。
- 前值使用调配前已留档的最近采样，后值使用本次采集的最近有效采样，页面显示实际采样时间。缺少数据或规格尚未生效时明确提示，不补造结果。
- 单次采样展示利用率变化、容量差和容量回收率，不计算P95、观察时长或累计容量时。
- 删除成效详情曲线图，保留前后对比表、关键成效数字及CSV/证据JSON导出。
- 旧默认窗口的未完成事件随下一次有效采集升级，旧政策保存在 `previous_policy`；已评估的历史结果保持冻结。

### 升级与验证

升级后重启应用并刷新浏览器，后续正常采集触发新评估逻辑；不需要重新执行已完成的调配。请保留服务器实际使用的运行配置、集群配置和 `outputs/` 数据（含成效数据库），不要用包内配置覆盖服务器配置。

编译、pyflakes、vulture80及成效相关54项Python测试、9项前端测试通过。当前本地运行配置下全量为663项通过、2项预测默认配置预期不匹配；仅在测试进程内使用测试默认预测配置后，全量665项、33项子测试通过。未修改磁盘上的运行配置；保留1条非有限值测试触发的NumPy警告。

部署包包含运行代码、静态资源、当前 `runtime_config.json` 和配置示例；不包含模拟数据、成效演示服务、文档、测试、虚拟环境或实际集群凭据。更新说明另附于ZIP旁。

## 2026-09-10：移除校准与验证

相较部署包 `resource_predict_20260910_221137.zip`：

- 删除资源详情“校准与验证”标签，详情保留“结论、指标、规格”。
- 删除校准上界曲线、反馈展示脚本和 `GET /api/resources/<id>/feedback` 接口。
- 删除经验上界计算、影子建议对照、启用评审、受控采用及仅供该功能使用的旧报告代码和性能基准。
- 删除 `archive_retention_days`、`calibrated_advice_enabled`、`calibrated_advice_resource_ids` 配置。
- 新预测不再生成校准、上界、影子对照、受控采用字段和对应阶段耗时。
- 同步清理专属测试与文档；独立预测准确性测试需要的旧数据库结构移到测试夹具。

正常预测、独立预测准确性统计、调配成效和模型路由实验保留。执行前的 `action_gate`、`confidence`、`data_quality`、`cooldown`、`policy_tier` 检查保留。

### 升级

部署包只包含运行文件及部署配置，不包含文档、虚拟环境、预测数据、测试和本地集群凭据。本文与压缩包旁的变更清单作为单独交付文档。

使用新目录部署或清理旧运行代码后再替换，避免直接覆盖解压留下已删除模块。迁移时保留实际使用的配置和数据，并刷新浏览器以加载新静态文件。

既有预测文件不会被本次代码删除自动改写；旧字段不再用于页面展示。下一次完整预测生成不含这些字段的新结果。原有数据和历史数据库未删除。

### 验证

- 编译检查、pyflakes、vulture（最低置信度 80）通过。
- 全量 Python 回归：622 项测试、33 项子测试通过；有 1 条非有限值测试触发的 NumPy 警告。
- 前端回归：39 项测试通过。
- 新产物不含校准字段；详情模板保留三个标签，已删除反馈接口返回 404。

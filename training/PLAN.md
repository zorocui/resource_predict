# 共享 LSTM 实验设计与实施计划

用户已确认共享 LSTM 对照实验，并要求独立训练目录、复用现有数据、不重新拉取。

采用 PyTorch 小型单变量共享 LSTM，按资源类型和容器/资源层级运行。
2026-09-13 用户确认所有现有指标共用一个模型，一次训练、批量输出各自预测；保留单指标对照选项。
默认 metric=all，归一化与产物标识包含资源、容器、指标，报告按指标分组，不合并原始曲线或分母。
复用 RawResourceStore 只读访问 raw_index.json + raw/，不导入采集器。
统一时间截止点切分训练、验证、测试；归一化仅拟合训练段。
窗口按需切片，不复制全部滑窗；缺口不插值，跳过不规则序列并记录原因。
验证选择 epoch，测试不参与选择；保存可加载权重、归一化参数、配置、输入指纹和逐窗口误差。
保留现有 Seasonal Naive、Rolling Mean，并支持 Prophet 对照。实验不更改生产模型配置。

实施顺序：
- [x] 数据读取、时间切分、惰性窗口和泄漏边界测试。
- [x] 训练 CLI、早停、模型留档和公平基线评价。
- [x] 本地既有 raw 小规模试跑，核对输入不变。
- [x] 使用说明、静态检查、相关回归和缓存清理。

新版 LSTM 将所选序列逐资源写为磁盘缓存，按需读取窗口；索引和评价明细仍驻留内存，不声称支持无界数据规模。

2026-09-13 用户追加 Prophet 持久化，并明确仅用于 training 和独立预测：
- 新增 training.prophet train/predict；沿用本地 raw，按序列拟合并以官方 JSON 格式存档。
- 加载预测明确不调用 fit，保留模型时间边界和失败原因；重训必须显式保存新版本。
- LSTM 的 --prophet-models 使用冻结对照，拒绝训练时间越过本实验训练截止点的模型。
- 验证模型重载、禁止 fit、身份与损坏/过期检查，以及评价泄漏保护；打包内网源码。

用户追加全量 Prophet 流式训练与断点续训：按资源读取（raw 缓存 1），逐模型原子检查点；保持预测索引格式。
续训核对配置、训练段指纹和模型 SHA256，仅重做缺失/变动/损坏项。开始/结束发布总清单，过程不做 O(n²) 重写；目录 OS 锁防并发。
覆盖真实小模型中断恢复、读入时机、零重复 fit、单序列变化/损坏重训、资源范围扩大和锁冲突测试。旧模型仍可预测，旧目录不具备检查点则明确拒绝续训。

用户追加 LSTM 同等能力：磁盘 .npy 缓存、紧凑 range 窗口索引、逐序列打乱采样；批次级原子 resume.pt 保存完整训练状态，默认每 1000 批次与轮次边界提交。
数据、配置、库版本校验后恢复；评价中断只重做评价。验证 CPU 上恢复前后训练曲线和权重逐项一致，保持四类指标共享模型与时间隔离，不改在线系统。
不执行全量超参搜索、不接入生产推理、不新增 TFT。用户要求的目录优先于通用文档位置惯例。

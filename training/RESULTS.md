# 2026-09-10 首轮本地流程验证

直接读取既有 `outputs/vm` 与 `outputs/k8s` 的小时级样本，没有拉取或生成新观测数据。
47 个 raw 索引/分片文件在实验前后的 SHA256 一致。数据覆盖 2025-01-01 至 2025-01-10，每条 240 点。
这是小样本流程验证，不是生产数据准确性结论，也不能用于推断万级资源吞吐。

两次运行均使用默认参数：输入/预测各 24 小时，hidden=32、1 层、dropout=0.1、学习率 0.001，20 轮上限，seed=42，CPU 2 线程。
所有序列统一训练截止 2025-01-09 00:00，验证截止 2025-01-10 00:00（均不含截止点），测试为 1 月 10 日。
每条序列一个测试窗口，测试标签不参与 epoch 选择。

| 实验 | 训练/验证/测试窗口 | LSTM RMSE | Seasonal Naive RMSE | Rolling Mean RMSE | Prophet RMSE |
| --- | --- | --- | --- | --- | --- |
| 4 个 VM，cpu | 100 / 4 / 4 | 0.106488 | 0.027233 | 0.049020 | 0.013918 |
| 4 个 Workload、9 个容器，cpu_request | 225 / 9 / 9 | 0.094154 | 0.029212 | 0.062352 | 0.018546 |

以上为平均窗口 RMSE，保留原指标比例尺度；各模型均无失败窗口。
两组 LSTM 最佳验证 epoch 均为 20，验证损失在上限处仍下降，不能宣称已充分收敛或参数最优。
本轮 LSTM 未优于任一基线，尚无替换生产模型的证据。后续可增加训练预算并只用验证集选择，但当前已查看的测试日期应视作开发证据，最终结论另留独立日期。

完整产物：
- `outputs/lstm/vm-cpu-20260910/`
- `outputs/lstm/k8s-cpu-20260910/`

每个目录包含 `model.pt`、`report.json`、`forecast_error_report.json`、`predictions.jsonl`。
运行命令为 README 的对应命令追加 `--max-resources 4`，输出目录分别使用上面的新目录。
训练依赖 PyTorch 2.14.0 来自阿里云镜像，下载包通过镜像索引中的 SHA256 校验。
检查结果：compileall、pyflakes、vulture（80）通过；训练/raw/原预测模型相关回归 51 passed，11 subtests passed。
